#include <stdio.h>

int main() {
    int num, soma = 0;

    printf("Digite 10 números: ");

    for (int i = 1; i <= 10; i++) {
        scanf("%d", &num);
        soma +=num;
    }

    float media = (float)soma / 10;
    printf("A média dos números digitados é %.2f", media);

    return 0;
}
